## ✅ Completed

* Hide the **notification system token** on all configuration pages (value not displayed).
* In the **Notifications** page, disable text fields when the notification system is disabled.
* Improve the **Overview** page UI.
* Refactor the overall layout for a cleaner UI.
* Add **snapshot** support for all notification systems (`/snapshot` or `/live` sends a live picture).
* Add **video** support for all notification systems (if possible), or at least provide a direct video link.
* Add an **application logging** system (with file saving).
* Log **all application actions**, with live log display in the UI (modern style consistent with other pages).
* Redesign the UI to be **more modern, beautiful, and responsive**.
* Implement **cooldown** in the Overview page.
* Add a **Test** button for each notification system on its settings page.

---

## 🚧 To Do

* Implement **LLM** (OpenAI / Gemini / Ollama) configuration for LLM-Vision snapshot interpretation — if enabled, this provides a description of the snapshot.
* Make the LLM system **fully configurable and optional**, with a dedicated configuration page.
* Add a **machine learning facial recognition** system with local storage of facial images for improved accuracy (self-hosted, easily integrable).
* Use LLM to determine the **action/event context** for more relevant notification messages.
* Add a UI for managing stored **images/faces** with First Name / Last Name labels.
* Add a **notification template customization** system (possibly a WYSIWYG editor) fully compatible with all implemented notification systems.

---

## 🎯 Per-Camera Settings

* Custom notification message.
* Object filtering.
* Time scheduling.
